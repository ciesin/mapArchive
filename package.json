{
  "name": "ciesin-map-archive",
  "private": true,
  "workspaces": [
    "packages/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/web",
    "preview": "npm run preview --workspace=packages/web"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "claude": "^0.1.1"
  }
}
