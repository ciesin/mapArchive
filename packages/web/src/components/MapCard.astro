---
interface Props {
  href: string;
  title: string;
  description?: string | null;
  theme?: string | null;
  spatialLevel?: string | null;
  datetime?: string | null;
  thumbnailUrl?: string | null;
}

const { href, title, description, theme, spatialLevel, datetime, thumbnailUrl } = Astro.props;
const dateStr = datetime ? new Date(datetime).getFullYear().toString() : '';
const badgeClass = theme ? `badge badge-${theme.replaceAll(':', '-')}` : 'badge';
---

<a href={href} class="card">
  <div class="card-image-wrapper">
    {thumbnailUrl ? (
      <img src={thumbnailUrl} alt={title} class="card-image" loading="lazy" />
    ) : (
      <div class="card-image card-image-placeholder"></div>
    )}
  </div>
  <div class="card-body">
    <h3 class="card-title">{title}</h3>
    <div class="card-meta">
      {theme && <span class={badgeClass}>{theme}</span>}
      {spatialLevel && <span class="card-tag">{spatialLevel}</span>}
      {dateStr && <span class="card-tag">{dateStr}</span>}
    </div>
    {description && <p class="card-desc">{description}</p>}
  </div>
</a>

<style>
  a.card {
    color: inherit;
    text-decoration: none;
  }
  .card-image-wrapper {
    overflow: hidden;
  }
  .card-image-placeholder {
    background: linear-gradient(135deg, #e2e8f0, #cbd5e1);
  }
  .card-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.375rem;
    margin-top: 0.25rem;
  }
  .card-tag {
    font-size: 0.75rem;
    color: var(--color-text-muted);
    background: var(--color-bg);
    padding: 0.0625rem 0.375rem;
    border-radius: var(--radius);
  }
  .card-desc {
    font-size: 0.8125rem;
    color: var(--color-text-muted);
    margin-top: 0.5rem;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
</style>
